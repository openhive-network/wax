[tool.poetry]
requires-poetry = "2.1.3"
name = "wax-local-tools"
version = "0.0.0"
description = ""
authors = []
packages = [
    { include = "wax_local_tools" },
]

[tool.poetry.dependencies]
python = ">=3.12,<4"


[build-system]
requires = ["poetry-core==2.1.3"]
build-backend = "poetry.core.masonry.api"
