[package]
name = "wax"
version = "0.1.0"
edition = "2024"

[dependencies]
wax_core = { path = "../wax_core" }
cxx = "1.0"
chrono = { version = "0.4", default-features = false, features = ["clock", "std"] }
thiserror = "2"
